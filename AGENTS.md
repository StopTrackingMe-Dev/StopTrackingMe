# Repository Instructions

## Git workflow

- 每次用户对话中的任务完成、准备发送最终答复前，如果仓库产生了与本次任务相关的文件变更，必须创建一个本地 Git commit。
- 提交前检查 `git status` 和待提交 diff，只纳入本次任务相关文件；不得覆盖、丢弃或顺带提交用户已有的无关改动。
- 提交前运行与改动风险相称的测试或检查，并在无法通过时如实说明。
- 使用简洁、能描述本次结果的提交信息。没有文件变更时不要创建空提交。
- 不得提交本机配置、构建产物、密钥、令牌或其他敏感信息。
- 默认只提交到本地仓库，不执行 `git push`；只有用户明确要求时才允许推送。

## 首页布局

- 保持首页干净：只展示核心操作和需要用户及时关注的状态。
- 设置类、一次性配置类功能应放在设置页面；只有可能因掉后台、手机重启等原因被取消、需要用户及时确认的状态（例如无障碍服务）才保留在首页。
